# Frontend production environment

# Backend base URL exposed from EC2
# If you only open port 8000 for backend, this stays with :8000.
# If later you proxy via Nginx and only expose HTTPS, you will change this.
NEXT_PUBLIC_API_BASE_URL=http://<EC2_PUBLIC_DNS_OR_IP>:8000
